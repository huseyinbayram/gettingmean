<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body>
    <% if ( userFullName ) { %>
        <h2>Hello, <%=userFullName%></h2>
        <a href="/signout">Sign out</a>
    <% } else { %>
        <a href="/signin">Sign in</a>
        <a href="/signup">Sign up</a>
    <% } %>
    <br>
    <img src="img/lena_color.gif" alt="logo">
</body>
</html>